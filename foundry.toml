[profile.default]
src = "src"
out = "out"
libs = ["lib"]
ffi = true
via_ir = true  # Required for snarkjs-generated verifier
optimizer_runs = 1  # Optimize for runtime size — Phase C additions push GuessGame past EIP-170 (24,576 bytes) at the default of 200. Hot-path gas stays competitive with via_ir on.
evm_version = "cancun"  # Base mainnet supports Cancun since Dencun upgrade (Mar 2024)
cbor_metadata = false  # Make bytecode deterministic for CREATE2 address prediction
bytecode_hash = "none"  # Required when cbor_metadata is false
remappings = [
    "@openzeppelin/contracts-upgradeable/=lib/openzeppelin-contracts-upgradeable/contracts/",
    "@openzeppelin/contracts/=lib/openzeppelin-contracts-upgradeable/lib/openzeppelin-contracts/contracts/"
]

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options

[invariant]
runs = 256           # Number of invariant test runs
depth = 15          # Number of calls per run
fail_on_revert = false  # Don't fail if handler functions revert
