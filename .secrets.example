# Copy this file to .secrets and add your deployment private key
# NEVER commit the actual .secrets file!

# Deployment private key (without 0x prefix)
PRIVATE_KEY=your_private_key_here_without_0x_prefix